// ui/components/nexora_icon.slint

import { NexoraTheme } from "theme.slint";

export component NexoraIcon inherits Image {
    in property <color> tint: NexoraTheme.text-muted;
    width: 20px;
    height: 20px;
    image-fit: contain;
    colorize: root.tint;
    accessible-role: none;
}
