// ui/components/rtl_card.slint

import { NexoraTheme } from "theme.slint";

export component RtlCard inherits Rectangle {
    background: NexoraTheme.surface;
    border-color: NexoraTheme.border;
    border-width: 1px;
    border-radius: 10px;
    @children
}
