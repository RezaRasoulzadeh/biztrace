// ui/components/rtl_text.slint

import { NexoraTheme } from "theme.slint";

export component RtlText inherits Text {
    horizontal-alignment: right;
    vertical-alignment: center;
    color: NexoraTheme.text;
    font-size: 14px;
    wrap: word-wrap;
}
