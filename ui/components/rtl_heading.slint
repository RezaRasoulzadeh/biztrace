// ui/components/rtl_heading.slint

import { BizTraceTheme } from "theme.slint";
import { RtlText } from "rtl_text.slint";

export component RtlHeading inherits RtlText {
    color: BizTraceTheme.text;
    font-size: 22px;
    font-weight: 700;
    wrap: no-wrap;
    overflow: elide;
}
