// ui/components/rtl_table_row.slint

import { BizTraceTheme } from "theme.slint";

export component RtlTableRow inherits Rectangle {
    height: 42px;
    background: BizTraceTheme.surface-raised;
    border-color: BizTraceTheme.border;
    border-width: 1px;
    border-radius: 7px;
    @children
}
