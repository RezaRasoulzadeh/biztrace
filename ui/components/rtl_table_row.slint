// ui/components/rtl_table_row.slint

import { NexoraTheme } from "theme.slint";

export component RtlTableRow inherits Rectangle {
    height: 42px;
    background: NexoraTheme.surface-raised;
    border-color: NexoraTheme.border;
    border-width: 1px;
    border-radius: 7px;
    @children
}
