// ui/components/rtl_table_header_cell.slint

import { BizTraceTheme } from "theme.slint";
import { RtlText } from "rtl_text.slint";

export component RtlTableHeaderCell inherits RtlText {
    color: BizTraceTheme.text-muted;
    font-size: 13px;
    font-weight: 600;
    wrap: no-wrap;
    overflow: elide;
}
